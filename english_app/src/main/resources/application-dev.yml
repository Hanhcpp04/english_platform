server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/english_app?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    username: english_user
    password: StrongPass!123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update     # lần đầu tạo bảng; về sau có thể đổi validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Email Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: nguyenvantien2004.vinhphuc@gmail.com  # Thay bằng email thật
    password: luuokfenovlrqykt    # Thay bằng app password của Gmail
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

# JWT Configuration
jwt:
  secret: mySecretKey123456789012345678901234567890123456789012345678901234567890
  expiration: 86400000  # 24 hours in milliseconds

# App Configuration
app:
  base-url: http://localhost:8081
